# class

## 概要

> [!TIP]
> 標準で実行されるプログラムは **最後のクラス名を`main`にしてください.**
> **`main`クラスは一つのプロジェクトに一つです.**

**class宣言**は、このプログラムが論理的にどの位置（名前空間・モジュール階層）に属するかを明示するための機能です.  
classは「関数や変数のグルーピング」「プログラムの構造整理」「名前空間の衝突防止」を目的とし、**継承やオーバーライド、インスタンス生成などのオブジェクト指向的な機能は持ちません**.

## 構文

`▻クラス名[.クラス名[.クラス名...]];`

- プログラムの先頭に**文として1行で宣言**します.
    
- 「.」で区切ることで**階層的な入れ子構造（ネスト）**を表現できます.
    

## 役割と特徴

- **論理的な位置付け**  
    プログラムやファイルがどのモジュール・名前空間に属するかを明示します.
    
- **関数・変数のグルーピング**  
    class宣言以降に定義された関数や変数は、そのclass階層に属します.
    
- **入れ子構造（ネスト）**  
    `▻A.B.C;` のように複数階層の名前空間を表現できます.
    
- **呼び出し規則**  
    他のclass階層から関数や変数を参照する場合は、  
    `A.B.C.関数名` のように完全修飾名で呼び出します.
    
- **継承・オーバーライドは非対応**  
    classはあくまで論理的なまとまりを示すものであり、クラス間の継承や多重継承、オーバーライドはサポートしません.
    

## 使用例

```
▻Math.Util; // 以降の関数や変数は Math.Util に属する 
◈⨊add≪∈a, ∈b≫⩿
	⨋retn≪∈a + ∈b≫; 
⪀ 

◈⨊square≪∈x≫⩿
	⨋retn≪∈x * ∈x≫; 
⪀
```

他のclass階層から呼び出す場合：

```
▻App.main;

◈∈ result = ∈⨊Math.Util.add≪∈3, ∈5≫;
⨋stut≪∈result≫;
```

## 注意事項

- **class宣言は1ファイルにつき1回、必ずプログラムの先頭に記述してください.**
    
- **classは論理的な区切りを与えるためのものであり、インスタンス生成や動的な型付けは行いません.**
    
- **class階層が深くなりすぎると可読性が下がるため、適切な粒度で設計してください.**
    

## 補足

- **class宣言は、他の言語における「名前空間」「モジュール」に近い役割です.**
    
- **classの入れ子構造は実行効率に大きく影響しませんが、設計・保守性のために適切に活用してください.**
    

このclass機能により、**大規模なプログラムでも構造化・整理が容易になり、名前空間の衝突を防ぎつつ、見通しの良いコード設計が可能**となります.
