# lambda 型 (LAMBDA)

lambda式.

定義：

```
lambda[<lambda式>]
```

関数を使った場合（l1には、関数がないので、python）

```python
def square(x):
    return x**2
y=square(2)
print(y)
```

lambdaを使った場合（l1）

```
%y = lambda[<x: x**2>]
stut(%y(2))
```
