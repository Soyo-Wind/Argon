# lambda 型 (LAMBDA)

lambda式.

定義：

```
lambda[<引数(,区切り):式>]
```

関数を使った場合（l2には、関数定義がないので、python）

```python
def square(x):
    return x**2
y=square(2)
print(y)
```

lambdaを使った場合（l2）（l関数を使います）

```
%y = lambda[<x>,<x 2 pow>]
$stut($l(%y,2))
```

#型 
