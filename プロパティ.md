# プロパティ機能

## 概要

**プロパティ**は、静的配列・List・strなどの組み込み型に対して、  
「値⁍プロパティ名」のArgon記法で属性情報（長さや状態）を取得するための機能です。  
プロパティは**読み取り専用**です。

## Argon記法

- プロパティアクセスは「変数⁍プロパティ名」の形式で記述します。
    
- 例：`arr⁍len`, `text⁍isnull`
    

## 提供されるプロパティ一覧

| 型    | プロパティ名 | 意味                | Argon記法例      |
| ---- | ------ | ----------------- | ------------- |
| str  | len    | 文字数               | `text⁍len`    |
| str  | isnull | nullまたは空strならtrue | `text⁍isnull` |
| 静的配列 | len    | 要素数               | `arr⁍len`     |
| 静的配列 | isnull | nullまたは要素数0ならtrue | `arr⁍isnull`  |
| List | len    | 要素数               | `list⁍len`    |
| List | isnull | nullまたは要素数0ならtrue | `list⁍isnull` |


- **Count/Capacity/First/Lastなど他のプロパティは提供しません。**
    
- **要素数はすべてlenで統一**し、C#のCountもlenに統合されています。
    
- **nullまたは空判定はisnullで統一**し、C#のIsNullOrEmptyに相当します。
    

## インデックスアクセス

- 配列・List・strなどは、**Pythonと同様に負のインデックスで末尾要素にアクセス可能**です。
    
    - `arr` … 先頭要素
        
    - `arr[-1]` … 最後の要素
        
    - `arr[-2]` … 後ろから2番目
        

## 使用例

```
class Example;
◈⫗ ⫗text = "Hello, world!";
⨋stut≪⫗text⁍len≫; // 13 
⨋stut≪⫗text⁍isnull≫;
// false 
⨋stut≪⫗text[0]≫;        // 'H' 
⨋stut≪⫗text[-1]≫;       // '!' 
◈⫗[] ⫗arr = ["A", "B", "C"]; stut≪⫗arr⁍len≫;// 3 
⨋stut≪⫗arr⁍isnull≫;     // false 
⨋stut≪⫗arr[-1]≫;        // "C" 
◈List ⫗list = []; 
⨋stut≪⫗list⁍isnull≫;    // true
```
