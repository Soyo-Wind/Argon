# マクロ

マクロを使うときは `(` の前に `#` を入れる

`.armc` がマクロ入れるファイルです.


`.armc` ファイル

```
(field1 (x))=>(field (a=x) (stut a))
```

これをプログラムに適用すると、`(field1 (1))` が `(field (a=1) (stut a))` になる.
また、上記のマクロの　`(field1 (x))` を `(field1 (<int>x))` とすると、型制限もできる.