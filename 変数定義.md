# 変数定義

変数は標準で不変です.初めて値がセットされた順にlockされます.
可変にするには、`⩽`を使います.
型マークに関しては[型マーク.mdを参照](型マーク.md)してください.

`◈`と型マークを組み合わせて使う
静的配列の場合は`∏`を使う
配列の場合は`∏{型マーク}11`（長さ11の配列）
Listは、`ɭ`を使う。

> [!TIP]
> 変数名は型マークを最初につけなくてはいけない


## 使用例

### Integer型のiという名前の変数宣言

``` plane
◈∈ ∈i;
```

### Char型のchararyという名前の長さ11の静的配列宣言

``` plane
◈∏⨝11 ∏⨝charary;
```

### 可変なByte型のmutbytという名前の変数宣言

``` plane
◈⩽∈ ⩽∈mutbyt;
```
