# 変数定義 (Create Queue)

変数は標準で可変です.
不変にするには、`^`を使います.
型マークに関しては[型マーク.mdを参照](型マーク.md)してください.

`cq`を使う

> [!TIP]
> 変数名は型マークを最初につけなくてはいけない


## 使用例

### iという名前の変数宣言

``` argon
(cq i '())
```

### 不変のmutbytという名前の変数宣言

``` argon
(cq mutbyt ^((⊀1) (∈102374)));
```

#定義系
