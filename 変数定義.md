# 変数定義

変数は標準で可変です.初めて値がセットされた順にlockされます.
不変にするには、`§`を使います.
型マークに関しては[型マーク.mdを参照](型マーク.md)してください.

`◈`と型マークを組み合わせて使う
静的配列の場合は`∏`を使い、`{長さ}∏{型マーク}` の形を使う
Listは、`ɭ`を使う。

> [!TIP]
> 変数名は型マークを最初につけなくてはいけない


## 使用例

### Integer型のiという名前の変数宣言

``` argon
◈∈i;
```

### Char型のchararyという名前の長さ11の静的配列宣言

``` argon
◈11∏⨝charary;
```

### 不変なByte型のmutbytという名前の変数宣言

``` argon
◈§∈imutbyt;
```

### str型のList

``` argon
◈ɭ⫗charary;
```
