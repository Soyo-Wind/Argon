#特殊式
# switch式

> [!TIP]
>  `_` はワイルドカードとして機能します.

構文：

```
{式}⨬{戻り値の型マーク}
⩿
    {switch条件式} => {switch対応式}{,(コンマ)で区切って記述}
⪀
```

## switch条件式

`₪` が switchの式 の値が入り、条件式（値はBoolで）を書くことができます.
## switch対応式

`₪` が switchの式 の値が入り、値（戻り値の型で）を書くことができます.
