# 標準関数

すべてのもとになる.

## 変数宣言兼変数GCの代わり `field`

変数(cons)宣言・初期値を入れたりできる.
型の制限を使って入るものに制限をつけたりもできる.

使用例：

```lisp
(field '(<int>x='(10))       ;この時点で変数xができる
    (progn (set x '(x "error"))    ;変数xは、int型に制限があるので、ここでエラー
    ((stut (+ x "aifh")))
)                            ;ここで変数xはまた宣言しない限り使えなくなる.
```

## 代入系 `set`

代入系の長、`car` とか `cdr` を使って、一部だけを変えることもできる.

使用例：

```scheme
(set x '(10))
(set (cdr x) '(10 20 30))
```

## 標準出力・エラー出力 `stut` `ster`

>[!TIP]
>Returnなどは、 [エスケープシーケンス](./エスケープシーケンス.md) を使用してください.

### 標準出力 `stut`

標準出力は、**stut関数** を使用できます.

使用例：

```scheme
(stut '("asdf"))
```

### 標準エラー出力 `ster`

標準エラー出力は、**ster関数** を使用できます.

使用例：

```scheme
(ster ^("boo!"))
```

## 繰り返し `while`

単純な繰り返しは **`while`** 関数を使います.
条件評価定義式は、条件式に含む変数を定義します.

構文：

```scheme
(while (条件式{bool}) 処理)
```

## ジャンプステーメント `break` `cont`

繰り返しに変化を持たせるときは、**`break` `cont`** 関数を関数を使います.

### `break`

**`break`** ステートメントはこれを囲むループ を終了させます。 

構文：

```scheme
(break)
```

### `cont`

**`cont`** ステートメントはこれを囲む これを囲むループ の新しい反復を開始します。 

構文：

```scheme
(cont)
```

## consの中からlambda式に当てはまるものを抜き出す `filter`

構文：

```scheme
(filter (lambda) (cons))
```

## マルチコア処理 `palmap`

### `palmap`

**map** の並列処理版.

## 大体のやつの源 `lambda`

ラムダ式は、引数に名前を付けて新しい関数を定義するための構文です.無名関数とも呼ばれ、関数規則には名前を付けません. `lambda`というキーワードを用いて定義します.
例として、引数xx、yyを用いた関数規則x+y×2x+y×2の定義は次のようになります.

```scheme
(lambda (x y) (+ x (* y 2)))
```

この関数規則を、たとえば引数xxを1010、引数yyを2020として評価する場合は、前節の式と同じく、ラムダ式を括弧の先頭に置いて表現します.

```scheme
((lambda (x y) (+ x (* y 2))) 10 20)
```

```
50
```

評価の様子を図として示すと、次のようになります.  
![](https://res.cloudinary.com/zenn/image/fetch/s--jDtRUq4U--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_1200/https://storage.googleapis.com/zenn-user-upload/deployed-images/0902fb5f1fb1ac591459a248.png%3Fsha%3D9d89866e20dee48d5f2590361960e55849173a86) 

先ほど、関数規則には名前を付けない、と述べましたが、実際には、**ラムダ式を値として扱うことができる**ため、ラムダ式を用いてラムダ式に名前を付けることが可能です.
次は、ラムダ式`(lambda (x y) (+ x (* y 2)))`に`f`という名前を付け、上記と同じ値を引数として渡して評価している例です.

```scheme
((lambda (f) (f 10 20)) (lambda (x y) (+ x (* y 2))))
```

![](https://res.cloudinary.com/zenn/image/fetch/s--SBU54rm7--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_1200/https://storage.googleapis.com/zenn-user-upload/deployed-images/5d2d995ac2580e6e41207869.png%3Fsha%3D5243002ac5507a20f99f9856fbb99b29c736ab86)

```
50
```

ラムダ式を値として扱うことができるということは、**関数出力としてラムダ式を返すことも可能**です.
次は、値として入力した`(lambda (x y) (+ x (* y 2)))`に引数xxとして値1010のみを渡し、もうひとつの値を引数として受け取る新しいラムダ式を定義して返すラムダ式です.

```scheme
((lambda (f) (lambda (y) (f 10 y))) (lambda (x y) (+ x (* y 2))))
```

ラムダ式のままでは計算結果が得られませんので、あらためてふたつ目の値2020を渡して評価している例が次の式および評価結果です.

```scheme
(((lambda (f) (lambda (y) (f 10 y))) (lambda (x y) (+ x (* y 2)))) 20)
```

![](https://res.cloudinary.com/zenn/image/fetch/s--jf6IWsUf--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_1200/https://storage.googleapis.com/zenn-user-upload/deployed-images/9857e7da5f001cb80d5bf522.png%3Fsha%3De717bbd7fe75633e21dbf56ada8baa008f48fa20)

```
50
```

ところで、では、改行と空白は無視されます（空行は評価することを示すものであるため、式の途中に入れることはできません）.
このことを利用して、次のように、改行と字下げで、ラムダ式の引数と規則本体を複数行に分けることで、少し見やすくすることができます.

```scheme
(((lambda (f)
    (lambda (y)
      (f 10 y)))
  (lambda (x y)
    (+ x (* y 2))))
 20)
```

## if

構文：

```scheme
(if 条件{bool} 真の場合の処理 偽の場合の処理)
```

「条件」が真ならば ... を、偽ならば ... の評価値を返します.

## 型変換 `convert`

構文：

``` scheme
(convert 変換前の値 変換先の型)
```

### 型変換できる種類

| ↓変換元,変換先→                    | [str](./型/str.md) | [byte](./型/数値型/整数数値型) | [int](./型/数値型/整数数値型) | [long](./型/数値型/整数数値型.md) | [float](./型/数値型/浮動小数点型.md) | [decimal](./型/数値型/浮動小数点型.md) | [bool](./型/bool.md) | [bigint](./型/数値型/bigint.md) |
| ---------------------------- | ----------------- | --------------------- | -------------------- | ------------------------ | -------------------------- | ---------------------------- | ------------------- | --------------------------- |
| [str](./型/str.md)            | /                 | ❌                     | ❌                    | ❌                        | ❌                          | ❌                            | ❌                   | ❌                           |
| [byte](./型/数値型/整数数値型)        | ✔️                | /                     | ✔️                   | ✔️                       | ✔️                         | ✔️                           | ❌                   | ✔️                          |
| [int](./型/数値型/整数数値型)         | ✔️                | ❌                     | /                    | ✔️                       | ✔️                         | ✔️                           | ❌                   | ✔️                          |
| [long](./型/数値型/整数数値型.md)     | ✔️                | ❌                     | ❌                    | /                        | ✔️                         | ✔️                           | ❌                   | ✔️                          |
| [float](./型/数値型/浮動小数点型.md)   | ✔️                | ❌                     | ❌                    | ❌                        | /                          | ✔️                           | ❌                   | ❌                           |
| [decimal](./型/数値型/浮動小数点型.md) | ✔️                | ❌                     | ❌                    | ❌                        | ❌                          | /                            | ❌                   | ❌                           |
| [bool](./型/bool.md)          | ✔️                | ✔️                    | ✔️                   | ✔️                       | ✔️                         | ✔️                           | /                   | ✔️                          |
| [bigint](./型/数値型/bigint.md)  | ✔️                | ❌                     | ❌                    | ❌                        | ✔️                         | ✔️                           | ❌                   | /                           |

## 型チェック `typeof`

[atom](./atom.md) が[何型なのか](./型/型一覧.md)を調べる.

## 名脇役 `progn`

複数の文を `while` や `if` に入れたいときは、prognを使います.

構文：

``` lisp
(progn 
	(処理)
	(2つ目もOK) 
	(3つ目もいくつでもprogn 閉じカッコまではTの時の処理) )
```

## 何かと使う `len`

consの要素数を出力

構文：

```scheme
(len (cons))
```

## Cons系

### Consの最初 `car`

与えられたconsのcar部を表示する.(atom)

構文：

``` scheme
(car (cons))
```

### Consのその他 `cdr`

与えられたconsのcar部を表示する.(cons)

構文：

``` scheme
(cdr (cons))
```

## Acell系

## Key→Value `ktv`

ConsのAcell部分からKeyが一致するAcellを取り出してValueをConsとして評価する.

構文：

``` scheme
(ktv (key) (AcellCons))
```

## Value→Key `vtk`

ConsのAcell部分からValueが一致するAcellを取り出してKeyをConsとして評価する.

構文：

``` scheme
(vtk (key) (AcellCons))
```


# 出典
https://zenn.dev/ytaki0801/books/funcationalprogramminglisp/viewer/02function_lambda

#関数系
