# 標準関数

## 標準入出力・エラー出力

### 標準入力 `stiln`

標準入力は、**stiln関数{[str型](./型/str.md)}** (Enter区切り).
char型で一文字ずつ使いたい場合は **sti関数{[char](./型/char.md)}** (一文字ずつ)を使います. [int](./型/数値型/整数数値型.md) や [double](./型/数値型/浮動小数点型.md) にしたい場合は [型変換](./型変換.md) を使用してください.

使用例：

```
⫗ strintxt = ⨊stiln;
```

### 標準出力 `stut`

標準出力は、**stut関数{[str型](./型/str.md)}** を使用できます.
[int](./型/数値型/整数数値型.md) や [double](./型/数値型/浮動小数点型.md) にしたい場合は [型変換](./型変換.md) を使用してください.
Returnなどは、 [エスケープシーケンス](./エスケープシーケンス.md) を使用してください.

使用例：

```
⨋stut≪"hey"≫;
```

### 標準エラー出力 `ster`

標準出力は、**ster関数{[str型](./型/str.md)}** を使用できます.
[int](./型/数値型/整数数値型.md) や [double](./型/数値型/浮動小数点型.md) にしたい場合は [型変換](./型変換.md) を使用してください.
Returnなどは、 [エスケープシーケンス](./エスケープシーケンス.md) を使用してください.

使用例：

```
⨋ster≪"oh, no!"≫;
```

## 繰り返し `for` `each`

### `for`

単純な繰り返しは **`for`** 関数を使います.
条件評価定義式は、条件式に含む変数を定義します.

構文：

```
⨋for≪条件評価定義式,条件式{bool},変化式{関数型}≫;
```

### `each`

`each` 関数は、**[静的配列](./型/型マーク.md)**, **[List](./型/型マーク.md)** を、小分け宣言式の変数に一つずつ代入します.
**[静的配列](./型/型マーク.md)**, **[List](./型/型マーク.md)** の要素にアクセスする場合に使用し、すべての要素を1回ずつ読み出すことができます.

構文：

```
⨋each≪条件評価定義式,条件式{bool},変化式{関数型}≫;
```

## ジャンプステーメント `break` `cont` `goto` 

繰り返しに変化を持たせるときは、**`break` `cont` `goto`** 関数を使います.

### `break`

**`break`** ステートメントはこれを囲む **`for`**, **`each`** ループ を終了させます。 

構文：

```
⨋break≪条件式{bool}≫;
```

### `cont`

**`cont`** ステートメントはこれを囲む これを囲む **`for`**, **`each`** ループ の新しい反復を開始します。 

構文：

```
⨋cont≪条件式{bool}≫;
```

### `goto`

**`goto`** ステートメントは、特定のラベルに制御を移します。 この特定のラベルは、同じ関数内に存在する必要があり、同じ関数内の 1 つのステートメントの前にのみ指定できます。

構文：

```
⨋goto≪ラベル名{str}≫;
```


#### ラベル

ステートメント ラベルは **`goto`** ステートメントに対してのみ意味を持ちます。他のコンテキストでは、ラベル付きステートメントはラベルに関係なく実行されます。

構文：

```
⌁ラベル名{str}
```

## マルチコア処理 `paleach`

### `paleach`

[each](### `each`) の並列処理版.

```
⨋for≪配列(またはList)名,変数名(型マーク付き(絶対)),変化式{関数型}≫;
```
