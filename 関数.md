# 関数

定義に関しては [関数定義を参照](./関数定義.md)

標準的な関数に関しては[標準関数を参照](./標準関数.md)

関数は、シグマを使います.
シグマの種類によって、戻り値を判定します.
`≪`と`≫`で囲んで中に引数を入れます.
引数がない場合は`≪`と`≫`は要りません.

## 戻り値なし

戻り値がない場合は`⨋`をシグマに使う

### 例：stut関数

``` plane
⨋stut≪"hey"≫;
```

## 戻り値あり

戻り値がない場合は`⨊`をシグマに使う

### 例：range関数

``` plane
⨊range≪11,1,1≫;
```
