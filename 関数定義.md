# 関数定義 (Create Sigma)

> ![IMPORTANT]
> 引数は、 `IN` キューに代入されます.
> 引数がない場合は、　`IN` キューには、 **空** が代入されます.

## 戻り値なし (Create Non return variable Sigma)

構文：

```plane
(cns 関数名 関数列)
```

## 戻り値あり (Create Return variable Sigma)

戻り値ありの時にしか使えなく、関数を起動したときに戻り値( `RETURN` キュー)を返します.

構文：

```
(crs 関数名 関数列)
```

#関数系 #定義系
